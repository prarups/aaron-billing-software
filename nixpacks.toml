[phases.setup]
aptPkgs = ["libcairo2", "pkg-config", "python3-dev"]

[phases.install]
cmds = ["python -m venv /app/.venv", "source /app/.venv/bin/activate && pip install -r requirements.txt"]

[phases.build]
cmds = ["python manage.py collectstatic --noinput"]
